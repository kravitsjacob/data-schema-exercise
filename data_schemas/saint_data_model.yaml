# ============================================================================
# Data Schema Sheet — SAInt Data Model
# ============================================================================
# Please fill out this sheet to describe your data schema / data model.
# This will be used for cross-project comparison at the G-PST workshop on
# power system planning interoperability.
#
# Instructions:
#   - Replace placeholder text (in angle brackets) with your information.
#   - Use ~ (null) for fields that don't apply.
#   - Use lists (- item) for multi-value fields. Please add entries as needed.
#   - Keep descriptions concise but specific.
#   - You do NOT need to detail things we can find in your repo (CI setup,
#     library dependencies, serialization formats, etc.). Just point us to
#     the code and we'll review it.
# ============================================================================

# ---------------------------------------------------------------------------
# 1. Identity
# ---------------------------------------------------------------------------
identity:
  schema_name: SAInt Data Model
  organization: encoord
  maintainers:
    - name: Jacob Kravits
      affiliation: encoord
      github: @kravitsjacob
      email: jacob@encoord.com
  repository: ~
  documentation: docs.encoord.com
  license: commercial
  version: 3.8
  maturity: Production

  # Point us to the code — we'll review the technical details ourselves
  link_to_schema_definition: https://docs.encoord.com/reference/latest/layers/index.html
  link_to_validation_logic: ~
  link_to_timeseries_management: https://docs.encoord.com/reference/latest/layers/scenario.html#reference-layers-scenario-scenario-profile
  link_to_entity_relation_diagram: https://docs.encoord.com/reference/latest/objects/electric.html

# ---------------------------------------------------------------------------
# 2. What It Is & What It Covers
# ---------------------------------------------------------------------------
summary:
  description: |
    The SAInt data model structures multi-energy infrastructure as networks
    (directed graphs of elementary and auxiliary objects with geometric,
    topological, relational, and static properties), scenarios (simulation type,
    time window, settings, controls, constraints, and profiles), and solutions
    (variables at each calculated time step; base results in files, derived
    results on demand). It supports integrated planning and analysis across
    electricity and gas for generation, transmission, and distribution
    planners who need one coherent representation instead of siloed sector
    data. Its users are industry professionals at utilities, in
    consulting, and at system operators who run simulations and optimizations
    and exchange network, scenario, and solution artifacts.

  modeling_domains_supported: |
    - Capacity Expansion
    - Production Cost (nodal)
    - Production Cost (zonal)
    - Balanced Three Phase AC Power Flow (steady-state)
    - Balanced Three Phase AC Power Flow (timeseries)
    - Unbalanced Three Phase AC Power Flow (steady-state)
    - Unbalanced Three Phase AC Power Flow (timeseries)
    - Steady-state Gas Hydraulic
    - Dynamic Gas Hydraulic
    - Thermal Hydraulic

  what_does_it_NOT_cover: |
    - Electric Dynamics (phasor/emt)

  data_captured: |
    - grid topology
    - device parameters
    - time series
    - investment costs
    - operating constraints

  conceptual_structure: |
    The network is a directed graph of typed elementary objects (nodes,
    branches, externals) plus auxiliary objects that subset or relate them,
    carrying geometric, topological, relational, and static properties. Equipment 
    types specialize through inheritance (e.g., a fuel generator is
    a generator, which is an external), so shared fields live on general types
    and specifics on subtypes—separate from how objects connect in the graph.
    Scenarios and time-step solutions layer on that same object graph, so the
    model is graph-based and object-oriented rather than a flat bus-table or
    purely relational layout.

# ---------------------------------------------------------------------------
# 3. Key Design Decisions
# ---------------------------------------------------------------------------
design:
  key_decisions:
    - decision: |
        Separate "what" from "how": the network holds the physical / structural
        "what" (topology, parameters, static properties); scenarios hold the
        mathematical and operational "how" (study type, solver assumptions,
        settings, controls, constraints, profiles).
      rationale: |
        Integrated planning workflows need to reuse one network across many
        studies while swapping or aligning assumptions between domains
        (e.g., electric vs gas vs thermal scenarios). Keeping the network
        stable and pushing formulation choices to the scenario layer lets
        teams move assumptions between scenarios without duplicating or
        rewriting the underlying asset model.

    - decision: |
        Separate base results from derived results: base results (fundamental
        outputs from the simulation or optimization) are persisted in the
        solution file; derived results are computed on demand from those bases
        (lazy evaluation) and are not pre-written to disk.
      rationale: |
        Users do not need to declare every quantity they might inspect before
        the run. They can execute a study, then explore charts, tables, and
        aggregates afterward without rerunning or resizing output upfront,
        at the cost of a short wait when a derived view is first requested.

  schema_format: ~

  implementation_languages: ~

  database_storage_backend: ~

  interoperability:
    imports_from:
      - ASCII templates
      - dot-raw
      - cyme
      - synergi gas
    exports_to:
      - ASCII templates
      - dot-raw
  data_tool_relation: Some tool specific

  extensibility: |
    The core schema is extended only by the vendor in product
    releases (no fork-and-modify). Types use specialization (subclassing)
    within the shipped software. Users extend behavior through the API and
    plugins (e.g., GUI visualizations), not by altering the persisted schema.

  units_handling: |
    <How are units handled? e.g., implicit SI, explicit per-field,
    unit conversion library, embedded in field names?>

  validation_approach: |
    <What does validation cover? e.g., schema structure only, range checks,
    cross-field validation, physical consistency checks (e.g., convexity)?>

  governance: |
    <Who decides what to include and when to accept changes? e.g., single
    maintainer, core team with RFC process, community PRs with review?>

# ---------------------------------------------------------------------------
# 4. Real-World Usage
# ---------------------------------------------------------------------------
usage:
  tools_built_on_schema:
    - tool: <e.g., PowerSimulations.jl>
      relationship: <e.g., Uses schema as standard input format>
      link: <https://github.com/...>

  largest_real_world_dataset: |
    <Describe the most complex real-world dataset successfully represented
    in your schema — system size, model type, data source, what was tested.>

  who_is_using_it:
    - <e.g., "NREL for ReEDS-to-Sienna production cost studies">
    - <...>

  data_available:
    - geographic_area: <e.g., US Western Interconnect>
      content: |
        <e.g., power flow only, investment cost data, unit commitment
        constraints on generators, load profiles only, etc.>
      access: <public | ceii_or_nda | licensed | proprietary>

# ---------------------------------------------------------------------------
# 5. Limitations & Challenges
# ---------------------------------------------------------------------------
challenges:
  known_limitations:
    - <e.g., "No native support for sector coupling / multi-carrier">
    - <...>

  hardest_problems_encountered: |
    <What has been the most difficult technical challenge in developing
    or using this data schema? What did you learn?>

# ---------------------------------------------------------------------------
# 6. Interoperability & Convergence
# ---------------------------------------------------------------------------
interoperability:
  areas_of_overlap_with_other_schemas: |
    <If you're familiar with any of the other data schemas in this comparison,
    note specific areas where your approaches overlap or diverge.>

  what_would_convergence_require: |
    <What would it take for you to align with or contribute to other data schemas if an
    interoperability-focused tool like a translator adopted a data schema as its core schema layer?
    What from your approach should still be incorporated?>

  biggest_thing_others_should_know: |
    <What is the single most important thing — positive or cautionary —
    that others should understand about your data schema?>

# ---------------------------------------------------------------------------
# Metadata
# ---------------------------------------------------------------------------
card_metadata:
  prepared_by: <Name>
  date: <YYYY-MM-DD>
  info_sheet_version: "1.0"
